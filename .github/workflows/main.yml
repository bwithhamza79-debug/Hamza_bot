name: Viral Shorts Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "0 12 * * *"

jobs:
  create-and-upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: sudo apt-get update && sudo apt-get install -y ffmpeg
      - run: pip install edge-tts requests google-api-python-client google-auth
      - name: Run bot
        env:
          YOUTUBE_REFRESH_TOKEN: ${{ secrets.YOUTUBE_REFRESH_TOKEN }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
        run: |
          python - <<'EOF'
          import os, tempfile, subprocess, random, asyncio, edge_tts, requests, math
          from google.oauth2.credentials import Credentials
          from googleapiclient.discovery import build
          from googleapiclient.http import MediaFileUpload

          REFRESH_TOKEN = os.environ["YOUTUBE_REFRESH_TOKEN"]
          CLIENT_ID = os.environ["GOOGLE_CLIENT_ID"]
          CLIENT_SECRET = os.environ["GOOGLE_CLIENT_SECRET"]

          # 1. Proven viral topic (no AI – always clean)
          viral_topics = [
              "iPhone 17 leaked features shock everyone",
              "TikTok ban update that changes everything",
              "Morning routine of millionaires exposed",
              "This 1‑minute trick saves your phone battery",
              "AI robot replaces human workers overnight",
              "Secret airplane hack flight attendants hate",
              "Food combination that melts belly fat",
              "World's most dangerous road revealed",
              "Why everyone is quitting their 9‑5 jobs",
              "Government declassified UFO footage",
              "Messi vs Ronaldo final debate ended",
              "Hidden Amazon button gives massive discount",
              "School lunch in Japan vs America",
              "Baby speaks first words in 5 languages",
              "Ocean discovered under Earth's crust",
              "Free electricity device shocks scientists",
              "Real reason celebrities delete Instagram",
              "McDonald's secret menu item confirmed",
              "Simple test predicts your lifespan",
              "New planet found with possible life"
          ]
          topic = random.choice(viral_topics)
          print(f"Topic: {topic}")

          # 2. Build viral narration from template
          print("Building narration...")
          hook = f"Did you know about {topic}? This is blowing up the internet right now."
          body = "People can't stop talking about it. Experts are shocked by what they discovered. The truth behind this will change everything you thought you knew."
          cta = "Like and subscribe for more trending updates!"
          narration = f"{hook} {body} {cta}"
          words = narration.split()
          if len(words) > 150:
              narration = ' '.join(words[:150])
          print(f"Narration: {narration[:120]}...")

          # 3. Generate 5 AI images directly from topic keywords
          print("Generating images...")
          image_prompts = [
              f"{topic}, dramatic reveal, 9:16 vertical",
              f"{topic}, close‑up, intense lighting",
              f"People reacting to {topic}, viral moment",
              f"Breaking news about {topic}, bold headline",
              f"{topic}, futuristic look, final shot"
          ]
          image_paths = []
          for prompt in image_prompts:
              url = f"https://image.pollinations.ai/prompt/{requests.utils.quote(prompt)}?width=576&height=1024&nologo=true"
              r = requests.get(url)
              tmp = tempfile.NamedTemporaryFile(suffix='.jpg', delete=False)
              tmp.write(r.content)
              tmp.close()
              image_paths.append(tmp.name)

          # 4. Voiceover (Jenny – most reliable)
          print("Generating voiceover...")
          audio_file = tempfile.NamedTemporaryFile(suffix='.mp3', delete=False)
          subs_file = tempfile.NamedTemporaryFile(suffix='.srt', delete=False)
          async def tts():
              communicate = edge_tts.Communicate(narration, "en-US-JennyNeural")
              submaker = edge_tts.SubMaker()
              with open(audio_file.name, 'wb') as f:
                  async for chunk in communicate.stream():
                      if chunk["type"] == "audio":
                          f.write(chunk["data"])
                      elif chunk["type"] == "WordBoundary":
                          submaker.feed(chunk)
              with open(subs_file.name, 'w') as f:
                  f.write(submaker.get_srt())
          asyncio.run(tts())

          # 5. Audio duration & trim to ≤60s
          result = subprocess.run(['ffprobe', '-v', 'error', '-show_entries',
                                   'format=duration', '-of', 'default=noprint_wrappers=1:nokey=1',
                                   audio_file.name], stdout=subprocess.PIPE, stderr=subprocess.STDOUT)
          dur = float(result.stdout)
          if dur > 60:
              trimmed_audio = tempfile.NamedTemporaryFile(suffix='.mp3', delete=False)
              subprocess.run(['ffmpeg', '-y', '-i', audio_file.name, '-t', '60', '-c:a', 'aac', trimmed_audio.name], check=True)
              audio_file = trimmed_audio
              dur = 60.0

          # 6. Build clean captions directly from narration words
          print("Generating captions...")
          words = narration.split()
          chunk_size = max(1, len(words) // math.ceil(dur)) if dur > 0 else 4
          srt_entries = []
          start = 0.0
          for i in range(0, len(words), chunk_size):
              segment = ' '.join(words[i:i+chunk_size])
              end = min(start + 2.5, dur)
              def fmt(t):
                  ms = int((t % 1) * 1000)
                  s = int(t % 60)
                  m = int((t // 60) % 60)
                  h = int(t // 3600)
                  return f"{h:02d}:{m:02d}:{s:02d},{ms:03d}"
              srt_entries.append(f"{len(srt_entries)+1}\n{fmt(start)} --> {fmt(end)}\n{segment}")
              start = end
          with open(subs_file.name, 'w') as f:
              f.write('\n\n'.join(srt_entries) + '\n\n')

          # 7. Silent slideshow
          print("Making video...")
          n = len(image_paths)
          dur_each = dur / n
          inputs = []
          filter_parts = []
          for i, img in enumerate(image_paths):
              inputs += ['-loop', '1', '-t', str(dur_each), '-i', img]
              filter_parts.append(f'[{i}:v]')
          silent = '/tmp/silent.mp4'
          concat = f"{' '.join(filter_parts)}concat=n={n}:v=1:a=0,format=yuv420p"
          subprocess.run(['ffmpeg', '-y'] + inputs + ['-filter_complex', concat, '-c:v', 'libx264', '-preset', 'ultrafast', silent], check=True)

          # 8. Burn captions + audio
          final = '/tmp/final.mp4'
          subprocess.run(['ffmpeg', '-y', '-i', silent, '-i', audio_file.name,
                          '-vf', f"subtitles={subs_file.name}:force_style='Fontsize=18,PrimaryColour=&H00FFFFFF,OutlineColour=&H00000000,BorderStyle=3'",
                          '-c:v', 'libx264', '-c:a', 'aac', '-shortest', final], check=True)

          # 9. YouTube metadata (always clean, ASCII safe)
          title = f"{topic} – You Won't Believe This!"
          title = title.replace('\n', ' ').replace('\r', ' ')[:100]   # ensure no newlines
          desc = f"Trending right now: {topic}. The truth will shock you. #viral #trending #shorts"
          tags = [t.strip() for t in topic.replace('–', '').split()[:5]] + ["viral", "trending", "breaking", "shorts", "youtube shorts"]

          # 10. Upload
          print("Uploading to YouTube...")
          creds = Credentials.from_authorized_user_info({
              'refresh_token': REFRESH_TOKEN,
              'client_id': CLIENT_ID,
              'client_secret': CLIENT_SECRET
          })
          youtube = build('youtube', 'v3', credentials=creds)
          body = {
              'snippet': {
                  'title': title,
                  'description': desc,
                  'tags': tags,
                  'categoryId': '22'
              },
              'status': {'privacyStatus': 'public'}
          }
          media = MediaFileUpload(final, chunksize=-1, resumable=True)
          youtube.videos().insert(part='snippet,status', body=body, media_body=media).execute()
          print("Upload complete! Check your channel.")
          EOF
